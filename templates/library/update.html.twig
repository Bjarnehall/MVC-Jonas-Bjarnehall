{% extends 'base.html.twig' %}

{% block title %}Update Book
{% endblock %}

{% block body %}
<div class="presentationbooks">

	<h1>Update Book</h1><br>

	<form method="post" action="{{ path('library_update', {'id': book.id}) }}" enctype="multipart/form-data">
		<input type="text" name="title" placeholder="Titel" value="{{ book.title }}">
		<input type="text" name="isbn" placeholder="ISBN" value="{{ book.isbn }}">
		<input type="text" name="author" placeholder="Författare" value="{{ book.author }}">
		<textarea name="description" placeholder="Beskrivning">{{ book.description }}</textarea>
		<input type="file" name="image">
		<button type="submit">Update</button>
	</form>
<div class="spaceblock"></div>
</div>
<script>
	document.body.style.backgroundImage = "url( {{ asset('img/biliotek.jpg') }})";
</script>

{% endblock %}
