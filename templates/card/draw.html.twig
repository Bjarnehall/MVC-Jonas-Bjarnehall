{% extends "base2.html.twig" %}

{% block title %}Game{% endblock %}

{% block main %}
<div class="hand">

<div class="deck">
<li>{{ card.getGraphicalRepresentation()|raw }}</li>
</div>
    <h3><a href="{{ path('draw_card') }}">Draw Another Card</a></h3>
    <h4>Cards Remaining in Deck: {{ remaining }}</h4>

</div>

{% endblock %}

